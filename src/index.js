import React from 'react';
import ReactDOM from 'react-dom/client';
import PickleMatch from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <PickleMatch />
  </React.StrictMode>
);